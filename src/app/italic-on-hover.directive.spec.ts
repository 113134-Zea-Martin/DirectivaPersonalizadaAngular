import { ItalicOnHoverDirective } from './italic-on-hover.directive';

describe('ItalicOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ItalicOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
